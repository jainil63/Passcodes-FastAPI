[project]
name = "passcodesfastapi"
version = "0.1.0"
description = "Custom backend for passcodes app"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[all]>=0.119.0",
]
license = "MIT"

